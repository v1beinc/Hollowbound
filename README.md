# Seedfall

Seedfall — это 2D idle simulation sandbox про маленькие абстрактные организмы-агенты, убежища, ресурсы, фракции и возникающее поведение.

Это новая версия проекта. Старый 3D-эксперимент сохранён отдельно как [Seedfall Prototype](https://github.com/v1beinc/Seedfall-Prototype).

## Что уже работает

- фиксированный simulation tick, независимый от FPS;
- тёмная карта сверху и процедурный seed;
- два цвета фракций и маленькие квадратные агенты;
- центральное убежище из стен с проёмом;
- добыча еды, возврат в убежище, запас и рождение новых агентов;
- пауза, ускорение времени и выбор агента мышью;
- базовая аналитика на экране: tick, население, еда, рождения, смерти и seed.

## Управление

- `Space` — пауза / продолжение;
- `Tab` — скорость `x1 → x5 → x25 → x100 → x500`;
- `ЛКМ` — выбрать агента и открыть его статистику;
- `Esc` — выйти.

## Стек

- .NET 10;
- C#;
- MonoGame 3.8.5.1 DesktopGL;
- Visual Studio 2026 Community или VS Code;
- встроенный bitmap UI без обязательных внешних графических ассетов.

## Запуск

```powershell
dotnet restore
dotnet run
```

В Visual Studio открой `Seedfall.sln` и запусти проект через `F5`.

## Архитектурное направление

Первый экран намеренно маленький и проверяемый. Он фиксирует основу симуляции: состояние агента, fixed tick, seed, ресурсы, убежище и наблюдение за конкретным агентом.

Цель больших популяций не достигается созданием и отрисовкой десятков миллионов полноценных объектов. Следующий масштабный слой должен разделить:

1. активных агентов рядом с камерой и событиями;
2. компактные данные агентов в массивах, чанках и spatial index;
3. агрегированные дальние популяции, которые просыпаются только при наблюдении или важном событии;
4. отдельный headless simulation runner для ночного фарма и сохранений.

Так можно прийти к большим популяциям, не превращая каждый кадр в перебор всех сущностей.
